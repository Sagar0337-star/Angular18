import { DatePipe1Pipe } from './date-pipe1.pipe';

describe('DatePipe1Pipe', () => {
  it('create an instance', () => {
    const pipe = new DatePipe1Pipe();
    expect(pipe).toBeTruthy();
  });
});
